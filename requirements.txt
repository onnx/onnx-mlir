lit~=15.0
# numpy 2.x removes legacy np.object, np.bool, np.float, np.complex, np.str,
# and np.int aliases; onnx-mlir now uses the underscore variants (np.bool_, etc.).
numpy~=2.2.0
protobuf>=6.33.5
onnx==1.20.1
pytest>=9.0.2
pytest-xdist>=3.4.0
